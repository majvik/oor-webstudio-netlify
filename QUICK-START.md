# Быстрый старт для разработки

> **Версия:** 1.0.0  
> **Дата:** 2025-01-16

## 🚀 Автоматическая настройка

Запустите скрипт настройки:

```bash
npm run setup
# или
bash scripts/setup-dev.sh
```

Скрипт автоматически:
- ✅ Проверит наличие Docker и Node.js
- ✅ Создаст необходимые директории
- ✅ Установит npm зависимости
- ✅ Запустит Docker контейнеры (если Docker установлен)

## 📋 Варианты запуска

### Вариант 1: Docker (рекомендуется)

**Требования:**
- Docker Desktop установлен и запущен

**Команды:**
```bash
# Настройка окружения
npm run setup

# Или вручную:
docker-compose up -d

# Проверка статуса
docker-compose ps

# Просмотр логов
docker-compose logs -f wordpress
```

**Доступ:**
- WordPress (HTTP): **http://localhost:8080**
- WordPress (HTTPS): **https://localhost:8443** (или https://localhost)
- phpMyAdmin: http://localhost:8081

**Важно:** для HTTPS используйте только порт **8443** (или 443). Если открыть **https://localhost:8080**, будет ERR_SSL_PROTOCOL_ERROR — на 8080 работает только HTTP.

**Первый запуск (HTTPS):** создайте сертификаты и поднимите контейнеры:
```bash
bash scripts/generate-local-certs.sh   # один раз: certs/local.crt, local.key
docker compose up -d
```
Откройте **https://localhost:8443**. Браузер покажет предупреждение о самоподписанном сертификате — для локалки можно нажать «Дополнительно» → «Перейти на сайт». HTTP (порт 8080) редиректит на HTTPS 8443.

**Синхронизация с продом (односторонняя):** см. [SAFETY-SYNC.md](SAFETY-SYNC.md) и скрипт `scripts/pull-prod.sh`.

**Остановка:**
```bash
docker-compose stop
```

### Вариант 2: Статический сервер (текущая версия)

**Команды:**
```bash
# Python
npm run serve
# или
python3 -m http.server 8040

# Node.js (альтернатива)
npm run serve:alt
# или
npx serve . -p 8040
```

**Доступ:**
- Сайт: http://localhost:8040

### Вариант 3: Альтернативные локальные серверы

Если Docker не установлен, используйте:

- **Local by Flywheel** (рекомендуется): https://localwp.com/
- **MAMP**: https://www.mamp.info/
- **XAMPP**: https://www.apachefriends.org/

## 🛠️ Полезные команды

### Docker команды

```bash
# Запуск
docker-compose up -d

# Остановка
docker-compose stop

# Перезапуск
docker-compose restart

# Просмотр логов
docker-compose logs -f wordpress

# Войти в контейнер
docker-compose exec wordpress bash

# Остановить и удалить (БД сохранится)
docker-compose down

# Остановить и удалить все (включая БД!)
docker-compose down -v
```

### Скрипты проекта

```bash
# Обновление версий кэша
npm run build

# Обработка артистов
npm run process-artists

# Миграция путей (dry-run)
npm run migrate-paths:dry-run

# Миграция путей (применить)
npm run migrate-paths
```

## 📁 Структура после настройки

```
/OOR-webstudio-netlify
├── wp-content/
│   └── themes/
│       └── oor-theme/     # Здесь будут PHP шаблоны
├── wordpress-uploads/      # Загруженные файлы WordPress
├── docker-compose.yml      # Конфигурация Docker
└── ...
```

## ⚠️ Troubleshooting

### Docker не запускается

1. Убедитесь, что Docker Desktop запущен
2. Проверьте порты 8080 и 8081 (не заняты ли)
3. Измените порты в `docker-compose.yml` если нужно

### Порт 8080 занят

Измените в `docker-compose.yml`:
```yaml
ports:
  - "8082:80"  # Вместо 8080
```

### Файлы не обновляются в контейнере

```bash
docker-compose restart wordpress
```

### Ошибки прав доступа

```bash
docker-compose exec wordpress bash
chown -R www-data:www-data /var/www/html/wp-content
chmod -R 755 /var/www/html/wp-content
```

## 📚 Дополнительная документация

- [WORDPRESS-SETUP.md](WORDPRESS-SETUP.md) - Подробная инструкция по установке
- [CURSOR-AGENT-GUIDE.md](CURSOR-AGENT-GUIDE.md) - Работа с агентом Cursor
- [README.md](README.md) - Общая документация проекта

---

**Готово к разработке!** 🎉
