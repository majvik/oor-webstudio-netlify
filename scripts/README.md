# Скрипты обработки

## process-artists.js

Скрипт для автоматической обработки артистов, их треков и обложек из исходной папки.

### Описание

Скрипт сканирует папку с артистами, обрабатывает изображения, конвертирует аудио файлы и генерирует HTML страницы для каждого артиста.

### Требования

- Node.js >= 14.0.0
- FFmpeg (установлен в системе, доступен в PATH)
- npm зависимости: `sharp`, `fluent-ffmpeg`

### Установка зависимостей

```bash
npm install
```

### Использование

**Обработка новых артистов:**
```bash
npm run process-artists
```

**Перекодирование всех существующих MP3 файлов:**
```bash
node scripts/process-artists.js --reencode-mp3
# или короткая версия
node scripts/process-artists.js -r
```

Эта команда перекодирует все существующие MP3 файлы для поддержки `Accept-Ranges: bytes`, что необходимо для работы перемотки в браузере.

### Структура исходных данных

Скрипт ожидает следующую структуру папок:

```
/Users/vik/Yandex.Disk.localized/Загрузки/1. Лейбл/Артисты/
├── CRYLOVE/
│   ├── Фото/
│   │   └── *.jpg, *.png, *.JPG, *.PNG (первое найденное изображение)
│   └── Релизы/
│       ├── CRYLOVE - Название трека 21.06.2024/
│       │   ├── *.wav (WAV файл трека)
│       │   └── *.jpg, *.png (обложка трека, первое найденное)
│       └── ...
├── Dimma Urih/
└── ...
```

### Что делает скрипт

1. **Обработка изображений артистов:**
   - Ищет первое изображение в папке `Фото/`
   - Ресайзит до 1280px по большей стороне (cover, сохраняет пропорции)
   - Создает версии 1x и 2x
   - Конвертирует в форматы: AVIF, WebP, PNG
   - Если изображение не найдено, создает серый плейсхолдер

2. **Обработка обложек треков:**
   - Ищет первое изображение в папке релиза
   - Ресайзит до 480px по большей стороне (cover, сохраняет пропорции)
   - Создает версии 1x и 2x
   - Конвертирует в форматы: AVIF, WebP, PNG
   - Если обложка не найдена, создает серый плейсхолдер

3. **Конвертация аудио:**
   - Конвертирует WAV файлы в MP3 (320kbps)
   - Автоматически перекодирует MP3 для поддержки `Accept-Ranges: bytes` (необходимо для перемотки)
   - Если в папке несколько WAV файлов (альбом), обрабатывает первый

4. **Генерация HTML страниц:**
   - Создает страницы в структуре `/artists/{slug}/artist.html`
   - Извлекает названия треков из названий папок
   - Извлекает год из даты в названии папки
   - Сортирует треки по дате (новые первыми)
   - Пропускает коллаборации (треки с запятой, где первый артист не совпадает)

### Выходная структура

```
public/assets/artists/
├── crylove/
│   ├── main.avif, main.webp, main.png (1x и 2x)
│   └── tracks/
│       ├── track-slug/
│       │   ├── cover.avif, cover.webp, cover.png (1x и 2x)
│       │   └── audio.mp3
│       └── ...
└── ...

artists/
├── crylove/
│   └── artist.html
└── ...
```

### Особенности

- **Транслитерация:** Названия артистов и треков автоматически транслитерируются в slug (кириллица → латиница)
- **Извлечение данных:**
  - Название трека: из названия папки (убирает префикс артиста и дату)
  - Год: из даты в формате DD.MM.YYYY в названии папки
  - Дата: для сортировки треков
- **Коллаборации:** Треки с запятой в названии обрабатываются только если первый артист совпадает с текущим
- **Альбомы:** Если в папке релиза несколько WAV файлов, обрабатывается первый
- **Поддержка перемотки:** Все MP3 файлы автоматически перекодируются с правильными заголовками ID3v2 для поддержки `Accept-Ranges: bytes`

### Настройка

Путь к исходной папке можно изменить в начале файла:

```javascript
const ARTISTS_SOURCE_DIR = '/Users/vik/Yandex.Disk.localized/Загрузки/1. Лейбл/Артисты';
```

### Логи

Скрипт выводит подробную информацию о процессе:
- ✓ Успешные операции
- ⚠️ Предупреждения (отсутствующие файлы)
- ⏭️ Пропущенные коллаборации
- ✗ Ошибки

### Пример вывода

```
🚀 Начало обработки артистов...

📁 Обработка артиста: CRYLOVE (crylove)
    ✓ Создано: main.avif
    ...
  🎵 Обработка трека: CRYLOVE - 3 дня комы 21.06.2024
    ✓ Создано: cover.avif
    ...
    ✓ Конвертировано в MP3: audio.mp3
  ✓ Обработано треков: 12
  ✓ Создана страница: /artists/crylove/artist.html

✅ Обработка завершена! Обработано артистов: 5
```

## prepare-main-page-artists.js

Скрипт для подготовки изображений артистов для слайдера на главной странице.

### Описание

Скрипт создает оптимизированные изображения для слайдера артистов на главной странице (`index.html`). Изображения создаются из исходных `main.png` файлов артистов с правильным кропом и размерами.

### Требования

- Node.js >= 14.0.0
- npm зависимости: `sharp`

### Установка зависимостей

```bash
npm install
```

### Использование

```bash
node scripts/prepare-main-page-artists.js
```

### Что делает скрипт

1. **Обработка изображений для слайдера:**
   - Берет исходные изображения из `public/assets/artists/{slug}/main.png`
   - Создает изображения размером 380×507px (1x) и 760×1014px (2x)
   - Соотношение сторон: 3:4 (соответствует CSS `aspect-ratio: 3 / 4`)
   - Кроп: `top center` (изображение позиционируется по верхнему краю и центру)
   - Конвертирует в форматы: AVIF, WebP, JPG
   - Сохраняет в `public/assets/img{1-5}.{avif,webp,jpg}` и `img{1-5}@2x.{avif,webp,jpg}`

2. **Список артистов:**
   - Скрипт обрабатывает артистов в следующем порядке:
     1. CRYLOVE → `img1.*`
     2. Dimma Urih → `img2.*`
     3. DSPRITE → `img3.*`
     4. NXN → `img4.*`
     5. Нет Времени Объяснять → `img5.*`

### Выходные файлы

```
public/assets/
├── img1.avif, img1.webp, img1.jpg (380×507px)
├── img1@2x.avif, img1@2x.webp, img1@2x.jpg (760×1014px)
├── img2.avif, img2.webp, img2.jpg
├── img2@2x.avif, img2@2x.webp, img2@2x.jpg
├── ... (img3, img4, img5)
```

### Настройка

Список артистов и их порядок можно изменить в начале файла:

```javascript
const ARTISTS = [
  { slug: 'crylove', name: 'CRYLOVE' },
  { slug: 'dimma-urih', name: 'Dimma Urih' },
  // ...
];
```

Размеры изображений соответствуют CSS стилям слайдера:
- Ширина: `min(380px, 100%)`
- Соотношение сторон: `3 / 4`
- Кроп: `top center` (позиционирование по верхнему краю и центру)

### Когда использовать

Запускайте скрипт после:
- Добавления новых артистов через `process-artists.js`
- Изменения исходных изображений артистов (`main.png`)
- Изменения списка артистов на главной странице

### Пример вывода

```
🚀 Подготовка изображений артистов для главной страницы...

Размеры: 380x507 (1x), 760x1014 (2x)
Кроп: top center (3:4 aspect ratio)

📸 Подготовка изображения для CRYLOVE (crylove)
  ✓ Создано: img1.avif
  ✓ Создано: img1.webp
  ✓ Создано: img1.jpg
  ✓ Создано: img1@2x.avif
  ✓ Создано: img1@2x.webp
  ✓ Создано: img1@2x.jpg

...

✅ Подготовка завершена! Обработано артистов: 5
```

### Примечания

- Скрипт перезаписывает существующие файлы `img{1-5}.*`
- Исходные изображения должны существовать в `public/assets/artists/{slug}/main.png`
- Если исходное изображение не найдено, скрипт выведет ошибку и пропустит артиста

