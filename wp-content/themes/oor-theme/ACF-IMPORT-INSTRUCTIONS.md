# Инструкция по импорту ACF Field Groups

> **Версия:** 1.0.0  
> **Дата:** 2025-01-16

Это руководство поможет вам импортировать ACF Field Groups в WordPress.

## 📋 Подготовка

Все JSON файлы Field Groups находятся в директории:
```
wp-content/themes/oor-theme/acf-json/
```

## 🔧 Способ 1: Автоматический импорт (рекомендуется)

ACF Pro автоматически загружает Field Groups из папки `acf-json/` при активации темы.

**Шаги:**
1. Убедитесь, что ACF Pro активирован
2. Активируйте тему `oor-theme`
3. Перейдите в **Custom Fields → Field Groups**
4. Field Groups должны появиться автоматически

**Если Field Groups не появились:**
- Используйте **Способ 3** (скрипт импорта) - самый надежный

## 🔧 Способ 2: Ручной импорт через админ-панель

Если автоматический импорт не сработал:

1. Перейдите в **Custom Fields → Tools**
2. Выберите вкладку **"Import Field Groups"**
3. Скопируйте содержимое JSON файла из `acf-json/`
4. Вставьте в поле **"Import"**
5. Нажмите **"Import"**

**Файлы для импорта:**
- `group_main_page.json` - Главная страница
- `group_artist_page.json` - Страница артиста
- `group_dawgs.json` - DAWGS
- `group_talk_show.json` - Talk-show
- `group_event.json` - Событие

## 🔧 Способ 3: Импорт через скрипт (САМЫЙ НАДЕЖНЫЙ)

Используйте готовый скрипт для автоматического импорта:

**Вариант A: Через браузер**
1. Убедитесь, что файл `import-acf-fields.php` находится в папке темы
2. Откройте в браузере: `http://localhost:8080/wp-content/themes/oor-theme/import-acf-fields.php`
3. Скрипт автоматически импортирует все Field Groups
4. **ВАЖНО: Удалите файл `import-acf-fields.php` после импорта!**

**Вариант B: Через WP-CLI (Docker)**
```bash
cd /Users/vik/Projects/OOR-webstudio-netlify
docker-compose exec wordpress wp --allow-root eval-file /var/www/html/wp-content/themes/oor-theme/import-acf-fields.php
```

**Вариант C: Через WP-CLI напрямую**
```bash
wp eval-file wp-content/themes/oor-theme/import-acf-fields.php
```

## ✅ Проверка после импорта

После импорта проверьте:

1. **Custom Fields → Field Groups** - все 5 групп должны быть видны
2. Откройте главную страницу в редакторе - должны появиться поля ACF
3. Откройте страницу артиста - должны появиться поля ACF
4. Откройте страницу DAWGS - должны появиться поля ACF
5. Откройте страницу Talk-show - должны появиться поля ACF
6. Откройте событие - должны появиться поля ACF

## 📝 Структура Field Groups

### 1. Главная страница (`group_main_page.json`)
- Hero Background Video
- Hero Modal Video
- Артисты в слайдере (Repeater)
- Talk-show изображения (Gallery)
- События (Repeater)
- Мерч изображения (Gallery)

**Location:** Page Template = `front-page.php` или Page Type = Front Page

### 2. Страница артиста (`group_artist_page.json`)
- Изображение артиста
- Краткое описание
- Полное описание
- Треки (Repeater)
- Социальные сети (Repeater)

**Location:** Post Type = `artist`

### 3. DAWGS (`group_dawgs.json`)
- Главное изображение
- Игроки (Repeater)
- Партнеры (Repeater)

**Location:** Page Template = `page-dawgs.php`

### 4. Talk-show (`group_talk_show.json`)
- Видео 1
- Видео 2
- Подкаст (Group: изображение, название, URL)

**Location:** Page Template = `page-talk-show.php`

### 5. Событие (`group_event.json`)
- Постер события
- Sold out
- Текст кнопки
- Ссылка на билеты
- Дата события
- Локация

**Location:** Post Type = `event`

## ⚠️ Важные замечания

1. **ACF Pro обязателен** - эти Field Groups используют функции Pro (Repeater, Gallery, Group)
2. **Порядок импорта не важен** - можно импортировать в любом порядке
3. **Дублирование** - если Field Group уже существует, ACF предложит синхронизировать
4. **Автосохранение** - после редактирования Field Group в админ-панели, изменения автоматически сохраняются в JSON файлы

## 🔄 Синхронизация

Если вы редактируете Field Groups в админ-панели:
- Изменения автоматически сохраняются в `acf-json/`
- JSON файлы обновляются автоматически
- Это позволяет версионировать Field Groups через Git

## 🐛 Устранение неполадок

**Проблема:** Field Groups не появляются после активации темы

**Решение:**
1. Проверьте, что ACF Pro активирован
2. Проверьте права доступа к папке `acf-json/`
3. Перейдите в **Custom Fields → Tools → Scan field groups**
4. Или импортируйте вручную через **Custom Fields → Tools → Import**

**Проблема:** Ошибка при импорте JSON

**Решение:**
1. Проверьте синтаксис JSON (используйте валидатор JSON)
2. Убедитесь, что используется ACF Pro (не Free версия)
3. Проверьте версию ACF Pro (должна быть 6.0+)

---

**Последнее обновление:** 2025-01-16
