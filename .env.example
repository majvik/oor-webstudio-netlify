# WordPress Configuration
# Скопируйте в .env: cp .env.example .env

# --- Docker (на VPS должен совпадать с уже созданными томами, иначе будет новая пустая БД) ---
# COMPOSE_PROJECT_NAME=oor-webstudio-netlify

# --- База данных (обязательно для docker-compose) ---
DB_NAME=wordpress
DB_USER=wordpress
DB_PASSWORD=wordpress
DB_ROOT_PASSWORD=rootpassword

# --- WordPress ---
WP_DEBUG=1
WP_SITE_TITLE=OOR
WP_ADMIN_USERNAME=your_username
WP_ADMIN_PASSWORD=your_strong_password
WP_ADMIN_EMAIL=your_email@example.com
# URL сайта: локально — localhost:8080, на VPS — ваш домен
WP_URL=http://localhost:8080

# --- Порты (на VPS обычно только 80 и 443) ---
HTTP_PORT=80
HTTP_ALT_PORT=8080
HTTPS_PORT=443
HTTPS_ALT_PORT=8443
PHPMYADMIN_PORT=8081
