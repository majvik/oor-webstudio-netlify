{
  "name": "oor-webstudio",
  "version": "1.0.0",
  "description": "Out of Records Web Studio",
  "scripts": {
    "build": "node scripts/update-cache-version.js",
    "generate-image-variants": "node scripts/generate-image-variants.js",
    "setup": "bash scripts/setup-dev.sh",
    "serve": "python3 -m http.server 8040",
    "serve:alt": "npx serve . -p 8040"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "dependencies": {
    "sharp": "^0.33.0",
    "fluent-ffmpeg": "^2.1.2"
  },
  "devDependencies": {
    "@types/fluent-ffmpeg": "^2.1.21"
  }
}

